first step for node
cd backend - npm init
then we install libraries

for react

cd frontend
npx create-react-app my-app


admin@software.com, pass: 123456


    // const handleDayClick = (newDay) => {
    //     const now = new Date();
    //     now.setHours(0, 0, 0, 0); // Set hours to 0 to compare only the date part
    //     if (newDay < now) {
    //         // Ignore clicks on past dates
    //         return;
    //     }
    //     setDays([...days, newDay]);
    // };